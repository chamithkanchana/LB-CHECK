const errorConverted = require('./error-converter');
const notFoundError = require('./error-not-found');
const errorHandler = require('./error-handler');

module.exports = {
  errorConverted,
  notFoundError,
  errorHandler,
};
